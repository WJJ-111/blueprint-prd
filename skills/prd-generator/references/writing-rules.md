# Writing Rules

- 只保留对产品、设计、研发交接有用的信息。
- 优先使用结构化表达，不把单条需求写成长段散文。
- 对重复信息只保留主表达，避免在多个章节反复出现。
- MVP 功能必须落为表格，便于后续拆任务和生成接口文档。
- 页面设计只描述视图、模块、关键交互和必要视觉方向。
- 技术规格里，已确认项直接写明，未确认但合理的实现方式需用“可”或“建议”表述。
- 缺失且重要的信息统一写“待确认”。
- 禁止把装饰性文案、营销口号、无约束价值判断写入 PRD。
